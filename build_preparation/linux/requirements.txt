# Use the PyTorch CPU index as the primary source
--index-url https://download.pytorch.org/whl/cpu

# Fallback to standard PyPI for other libraries
--extra-index-url https://pypi.org/simple

# Pin PyTorch and Torchvision (note the +cpu suffix)
torch==2.8.0+cpu
torchvision==0.23.0+cpu

# Install from GitHub
auto_LiRPA @ git+https://github.com/Verified-Intelligence/auto_LiRPA

# Standard libraries
tqdm
onnx2pytorch
pyside6 >= 6.10.1
onnxruntime >= 1.23.0
numpy >= 2.2.6
matplotlib >= 3.10.8
watchdog >= 6.0.0
onnx >= 1.20.0
